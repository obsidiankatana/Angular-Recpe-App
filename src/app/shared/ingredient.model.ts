export class Ingredient {
  // this declares the public variables in the class and the constructor all in one line;
  // no need to reapeat yourself
  constructor(public name: string,public amount: number) {}
}
